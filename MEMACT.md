# Memact Inference Notes

Inference semantically understands capture events.

It chooses which captured activity has enough meaning to become semantic
evidence. Future local model or LLM integrations belong here as optional
semantic providers.

Inference does not capture activity, form durable schemas, store memory, or run
features.
